# iochiken

Requirements
	node v10

Install ionic
	npm install -g @ionic/cli

Create server
	ionic serve --external

Create page

	ionic generate page tab4detalle

Packages installed!

	npm install @agm/core --save